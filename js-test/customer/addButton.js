/* There should be an add to cart button on the product-details page. This will toggle the product in and out 
   of a cart array stored in local storage. */ 

   const addButton = document.querySelector("#cart");

   addButton.addEventListener("click", addItem);

   function addItem() {

      const details.push
   }